---
description: List all indexed library stores
allowed-tools: [Bash(*)]
---

List all indexed knowledge stores:

```bash
node ${CLAUDE_PLUGIN_ROOT}/dist/index.js stores
```

The output above shows all indexed stores with their names, types, IDs, and paths.
